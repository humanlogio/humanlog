build:
  main: ./cmd/humanlog/main.go
  binary: humanlog
  ldflags:
    - -s -w -X main.Version={{.Version}}
  goos:
    - windows
    - darwin
    - linux
  goarch:
    - amd64
    - arm64

brews:
  - tap:
      owner: humanlogio
      name: homebrew-tap

nfpms:
  - maintainer: 'antoinegrondin@gmail.com'
    formats:
      - deb
